spring:
    thymeleaf:
        cache: false
        check-template-location: true
        prefix: classpath:/templates/
        suffix: .html
        servlet:
          content-type: text/html
server:
  servlet:
    context-path: /
  port: 8084